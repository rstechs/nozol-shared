{
  "name": "@rstechs/nozol-shared",
  "version": "1.0.1",
  "description": "Nozol shared module",
  "main": "index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "test": "",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "preversion": "echo pre version",
    "version": "echo version",
    "postversion": "echo post version"
  },
  "author": "rstech",
  "license": "",
  "dependencies": {
    "@sentry/node": "^6.2.0",
    "@sentry/tracing": "^6.2.0",
    "amqplib": "^0.5.2",
    "axios": "^0.21.1"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.6.2"
  }
}
